# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  email_provider_username: mikabajic@gmail.com
  email_provider_password: 'milena@1957'
  mailchimp_api_key: 'c530a338f5593c17f80e170bc69bbd0f-us8'
  mailchimp_list_id: 'a5a88a903d'
  domain_name: example.com
  owner_email: mikabajic@gmail.com
  secret_key_base: 3007d189aca32490d9d153ef290ec1b1f0f8d961073adffbbc22b33c7f1694ea6e513f3c937fc85333bf79e744b6e1069d6b0cbeb5d6237927ddeea925decefe

test:
  secret_key_base: 3007d189aca32490d9d153ef290ec1b1f0f8d961073adffbbc22b33c7f1694ea6e513f3c937fc85333bf79e744b6e1069d6b0cbeb5d6237927ddeea925decefe

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  mailchimp_api_key: <%= ENV["MAILCHIMP_API_KEY"] %>
  mailchimp_list_id: <%= ENV["MAILCHIMP_LIST_ID"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  owner_email: <%= ENV["OWNER_EMAIL"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>